# ⚡ CORS — Шпаргалка

## 🎯 Что такое CORS в 1 минуту

**CORS** = Cross-Origin Resource Sharing

**Простыми словами:** Правила, разрешающие одному сайту загружать ресурсы с другого сайта.

---

## 🚦 Когда возникает проблема

```
Ваш сайт: https://figma.com
Изображение: https://example.com/image.jpg
                    ↓
Браузер блокирует: ❌ CORS policy error
```

---

## ✅ Решение: Сервер добавляет заголовок

```http
Access-Control-Allow-Origin: *
```

Теперь: ✅ Работает!

---

## 📊 Быстрая проверка

```bash
# Проверить, есть ли CORS:
curl -I https://example.com/image.jpg | grep -i "access-control"

# Если видите это — всё ОК:
Access-Control-Allow-Origin: *
```

---

## 🎓 В нашем плагине

### Было (думали нужен proxy):
```
Figma → corsproxy.io → Яндекс CDN
        ⚠️ медленно
```

### Стало (обнаружили CORS):
```
Figma → Яндекс CDN
✅ быстро, работает!
```

---

## 🔑 Главное

1. **CORS** = разрешение сервера на загрузку ресурсов
2. **Яндекс CDN** уже имеет CORS ✅
3. **Proxy не нужен** 🎉

---

## 📚 Подробнее

См. [WHAT_IS_CORS.md](./WHAT_IS_CORS.md)

# Инструкция по запуску SnapFill

## ✅ Проект собран и готов к тестированию!

### 📁 Структура проекта

```
SnapFill/
├── dist/              # Скомпилированные файлы плагина
│   ├── code.js       # ✅ Готов
│   ├── ui.html       # ✅ Готов
│   └── ui.css        # ✅ Готов
├── src/              # Исходники
├── test-data.json    # 📄 Тестовые данные (объект)
├── test-data.csv     # 📄 Тестовые данные (CSV)
├── test-data-array.json  # 📄 Тестовые данные (массив)
└── manifest.json     # Манифест плагина
```

---

## 🚀 Запуск в Figma

### Шаг 1: Загрузить плагин в Figma Desktop

1. Откройте **Figma Desktop** (не браузерную версию!)
2. Откройте любой файл или создайте новый
3. В меню выберите:
   - **Plugins** → **Development** → **Import plugin from manifest...**
4. Выберите файл `manifest.json` в корне проекта SnapFill
5. Плагин загружен! ✅

### Шаг 2: Подготовить тестовый макет

Создайте простой фрейм с элементами:

```
Frame "ProductCard"
├── Text "title"           ← текстовый слой
├── Text "subtitle"        ← текстовый слой
├── Text "price"           ← текстовый слой
├── Text "description"     ← текстовый слой
├── Text "badge"           ← текстовый слой
├── Text "rating"          ← текстовый слой
└── Rectangle "image"      ← прямоугольник для изображения
```

**Важно:** Имена слоев должны точно совпадать с полями в JSON/CSV!

### Шаг 3: Запустить плагин

1. Выделите созданный фрейм "ProductCard"
2. В меню: **Plugins** → **Development** → **SnapFill**
3. В открывшемся окне нажмите **"Выбрать файл"**
4. Выберите один из тестовых файлов:
   - `test-data.json` - один продукт
   - `test-data.csv` - список продуктов (будет взята первая строка)
   - `test-data-array.json` - массив продуктов (будет взят первый элемент)
5. Увидите превью данных
6. Нажмите **"Заполнить"**
7. Готово! 🎉

---

## 📝 Формат тестовых данных

### test-data.json (объект)
```json
{
  "title": "iPhone 15 Pro",
  "subtitle": "Титан. Такой прочный...",
  "price": "от 99 990 ₽",
  "image": "https://images.unsplash.com/..."
}
```

### test-data.csv (таблица)
```csv
title,subtitle,price,image
iPhone 15 Pro,Титан...,от 99 990 ₽,https://...
MacBook Pro 16,Феноменальная...,от 299 990 ₽,https://...
```

### test-data-array.json (массив)
```json
[
  { "title": "iPhone 15 Pro", ... },
  { "title": "MacBook Pro 16", ... }
]
```

---

## 🎨 Пример макета для быстрого теста

1. Создайте Frame (F)
2. Добавьте несколько Text слоев (T)
3. Назовите их: `title`, `price`, `badge`, `rating`
4. Добавьте Rectangle (R) и назовите `image`
5. Выделите весь Frame
6. Запустите плагин и загрузите `test-data.json`

---

## 🔧 Режим разработки

Если нужно внести изменения:

```bash
# 1. Редактируйте файлы в src/
# 2. Пересоберите проект:
npm run build

# 3. В Figma: Plugins → Development → Hot reload plugin (⌘⌥P)
```

---

## ❓ Возможные проблемы

### Плагин не запускается
- Убедитесь, что используете **Figma Desktop**, а не браузер
- Проверьте, что проект собран (`npm run build`)
- Перезапустите Figma

### Данные не заполняются
- Проверьте, что фрейм выделен перед запуском плагина
- Убедитесь, что имена слоев точно совпадают с полями в данных
- Откройте DevTools в Figma (Help → Toggle Developer Tools) для отладки

### Изображения не загружаются
- Проверьте, что URL изображения доступен
- Figma требует HTTPS для загрузки изображений
- Убедитесь, что слой для изображения - Rectangle или Frame

---

## 📞 Готово к тестированию!

Все файлы созданы, плагин собран. Можно загружать в Figma и тестировать! 🚀

